<div class="card">
  <div class="card-header">
    <h2><%= release.title %></h2>
  </div>
  <div class="card-description">
    <% @releases.each do |release| %>
      <% if release.record %>
        <p><%= release.record.name %></p>
      <% end %>
    <% end %>
  </div>
</div>
